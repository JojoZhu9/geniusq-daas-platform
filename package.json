{
  "name": "geniusq-daas-platform-site",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "npm --prefix frontend install --no-audit --no-fund && npm --prefix frontend run build:site && node tools/copy-site-dist.mjs"
  },
  "devDependencies": {}
}
